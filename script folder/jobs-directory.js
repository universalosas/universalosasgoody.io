
    // Select the div by class name
    const myDiv = document.querySelector(".page-nav1");

    // Add a click event
    myDiv.addEventListener("click", () => {
        // Redirect to another page
        window.location.href = "universal-game-pages.html";
    });

      // Select the div by class name
      const myDiv2 = document.querySelector(".page-nav2");

      // Add a click event
      myDiv2.addEventListener("click", () => {
          // Redirect to another page
          window.location.href = "osas-goody-page.html";
      });

        // Select the div by class name
    const myDiv3 = document.querySelector(".page-nav3");

    // Add a click event
    myDiv3.addEventListener("click", () => {
        // Redirect to another page
        window.location.href = "universal-game-pages.html";
    });

      // Select the div by class name
      const myDiv4 = document.querySelector(".page-nav4");

      // Add a click event
      myDiv4.addEventListener("click", () => {
          // Redirect to another page
          window.location.href = "UniTech-page.html";
      });